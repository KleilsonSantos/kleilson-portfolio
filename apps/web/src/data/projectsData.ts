import type { Project } from '../types'
import data from '../../content/projects.json'

/** Fonte: `apps/web/content/projects.json` (Decap CMS / Content-as-Code). */

export const PROJECTS: Project[] = data.projects
